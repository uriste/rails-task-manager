<% @tasks.each do |task| %>
  <%= link_to task.title, "http://localhost:3000/tasks/#{task.id}" %>
  <%= link_to "Delete", task_path(task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
<% end %>


  <%= link_to "New Task", "http://localhost:3000/tasks/new" %>
